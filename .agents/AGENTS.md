# Project Agent Rules

## Git & Version Control Workflow
- **Always create a separate git feature branch** before committing and pushing changes to GitHub instead of pushing directly to `main`.
  - Example: `git checkout -b feature/<descriptive-branch-name>` before pushing to origin.
