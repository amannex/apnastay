# Project Agent Rules

## Git & Version Control Workflow
- **Always create a separate git feature branch** for changes instead of committing directly to `main`.
  - Example: `git checkout -b feature/<descriptive-branch-name>`
- **Do not push to remote until explicitly told**: Only create local commits. Never run `git push` unless the user specifically asks to push.
